import { ActivityMap } from '../../types';

// Indian-themed activities (color the diya, rangoli, dosa, etc.)
// These overlay on top of universal activities when Hindu is the dominant tradition

export const hinduActivities: ActivityMap = {
  // TODO: Extract from v1 App.tsx
};
